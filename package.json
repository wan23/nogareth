{
  "name": "nogareth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
"files": [
        "game/",
        "index.html"
    ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve index.html",
    "build": "rm -rf dist;  parcel build index.html game/index.html --no-source-maps; cp -r game/data dist/game",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^5.2.0",
    "gh-pages": "^1.2.0",
    "parcel-bundler": "^1.9.7",
    "typescript": "^2.9.2"
  }
}
